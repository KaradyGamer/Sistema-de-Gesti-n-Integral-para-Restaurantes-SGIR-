# ========================================
# SGIR - Configuración del Sistema (PLANTILLA)
# ========================================
# IMPORTANTE:
# 1. Copia este archivo como .env
# 2. Genera nueva SECRET_KEY con:
#    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# 3. Reemplaza TODOS los valores de ejemplo
# 4. NUNCA subas el archivo .env al repositorio
# ========================================

# === CONFIGURACIÓN GENERAL ===
SECRET_KEY=GENERAR-NUEVA-KEY-AQUI-NO-USAR-ESTA
DEBUG=True

# Hosts permitidos (separados por coma, sin espacios)
# Desarrollo: localhost,127.0.0.1
# Producción: tudominio.com,www.tudominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# === SEGURIDAD Y CORS ===
# Orígenes permitidos para CORS (separados por coma)
# Desarrollo: http://localhost:8000,http://127.0.0.1:8000
# Producción: https://tudominio.com
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Orígenes confiables para CSRF (separados por coma)
# Desarrollo: http://localhost:8000,http://127.0.0.1:8000
# Producción: https://tudominio.com
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# === COOKIES Y SESIONES ===
# Desarrollo: HttpOnly=True siempre, Secure=False (sin HTTPS)
# Producción: HttpOnly=True, Secure=True (con HTTPS)
SESSION_COOKIE_HTTPONLY=True
CSRF_COOKIE_HTTPONLY=True
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SAMESITE=Lax
CSRF_COOKIE_SAMESITE=Lax

# === BASE DE DATOS ===
# Desarrollo: SQLite (configuración automática)
# Producción: PostgreSQL (descomentar y configurar abajo)
DATABASE_URL=sqlite:///db.sqlite3

# PostgreSQL para producción (descomentar y configurar):
# DATABASE_URL=postgresql://usuario:password@localhost:5432/sgir_db

# === CÓDIGOS QR ===
# URL del servidor para códigos QR (cambiar según entorno)
# Desarrollo: localhost:8000 o tu IP local
# Producción: tudominio.com
QR_HOST=localhost:8000

# === JWT TOKENS ===
# Tiempo de vida de tokens de acceso (en minutos)
# Desarrollo: 60 minutos
# Producción: 15-30 minutos (más seguro)
JWT_ACCESS_TOKEN_LIFETIME=60

# Tiempo de vida de tokens de refresco (en días)
# Desarrollo: 14 días
# Producción: 7 días
JWT_REFRESH_TOKEN_LIFETIME=14

# ========================================
# CONFIGURACIÓN DE PRODUCCIÓN
# ========================================
# Descomentar estas líneas cuando DEBUG=False:
#
# DEBUG=False
# ALLOWED_HOSTS=tudominio.com,www.tudominio.com
# CORS_ALLOWED_ORIGINS=https://tudominio.com
# CSRF_TRUSTED_ORIGINS=https://tudominio.com
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SESSION_COOKIE_SAMESITE=Strict
# CSRF_COOKIE_SAMESITE=Strict
# DATABASE_URL=postgresql://usuario:password@localhost:5432/sgir_db
# QR_HOST=tudominio.com
# JWT_ACCESS_TOKEN_LIFETIME=15
# JWT_REFRESH_TOKEN_LIFETIME=7
