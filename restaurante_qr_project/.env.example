# ========================================
# CONFIGURACIÓN DE EJEMPLO - Restaurante QR
# ========================================
# IMPORTANTE:
# 1. Copia este archivo como .env
# 2. Reemplaza los valores de ejemplo con los reales
# 3. NUNCA subas el archivo .env al repositorio
# ========================================

# Django Secret Key
# Genera una nueva con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=tu-clave-secreta-aqui-cambiar-en-produccion

# Modo de desarrollo (True solo en desarrollo local)
DEBUG=True

# Hosts permitidos (separados por comas, sin espacios)
ALLOWED_HOSTS=localhost,127.0.0.1

# Base de datos (SQLite por defecto)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3

# CORS - Orígenes permitidos (separados por comas)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ========================================
# CONFIGURACIÓN DE PRODUCCIÓN (descomentar y configurar)
# ========================================
# DEBUG=False
# ALLOWED_HOSTS=tu-dominio.com,www.tu-dominio.com
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_SSL_REDIRECT=True
# SECURE_HSTS_SECONDS=31536000

# Base de datos PostgreSQL (recomendado para producción)
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=restaurante_db
# DB_USER=postgres
# DB_PASSWORD=tu-password-seguro
# DB_HOST=localhost
# DB_PORT=5432
